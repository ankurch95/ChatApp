import { View, Text } from 'react-native'
import React from 'react'
import { TabStackParamList } from '../../navigation/AppStackNavigator'

export const ProfileScreen = ({ route, navigation }: TabStackParamList<'ProfileScreen'>) => {
    return (
    <View>
      <Text>index</Text>
    </View>
  )
}